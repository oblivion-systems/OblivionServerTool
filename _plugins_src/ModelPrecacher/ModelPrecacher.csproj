<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <AssemblyName>ModelPrecacher</AssemblyName>
    <RootNamespace>ModelPrecacher</RootNamespace>
    <!-- Build against the server's own CounterStrikeSharp assemblies so the
         plugin is ABI-identical to the installed runtime (CSS 1.0.368).
         Override CssApi at build time via MSBuild property or env-var, e.g.:
           dotnet build /p:CssApi="C:\path\to\counterstrikesharp\api"
         Default below assumes steamcmd installed at C:\steamcmd. -->
    <CssApi Condition="'$(CssApi)' == ''">C:\steamcmd\steamapps\common\Counter-Strike Global Offensive\game\csgo\addons\counterstrikesharp\api</CssApi>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reference every CSS api DLL; Private=false keeps build output to just
         ModelPrecacher.dll (the host already provides these at runtime). -->
    <Reference Include="$(CssApi)\*.dll">
      <Private>false</Private>
    </Reference>
  </ItemGroup>

</Project>
